# 🎯 Day 14 — SQL Server Date Functions: FORMAT, DATEADD, DATEDIFF & EOMONTH

## 💼 Interview Questions

### 1. What is the FORMAT() function?
`FORMAT()` is used to display a date or value in a specified format.

```sql
SELECT FORMAT(GETDATE(), 'dd-MM-yyyy');
