# Day 08 — Quick Notes

## Operators

| Type | Operators |
|---|---|
| Arithmetic | +, -, *, /, % |
| Relational | <, >, <=, >=, =, <>, != |
| Logical | AND, OR, NOT |
| Special | BETWEEN, IN, LIKE, IS, ANY, ALL, EXISTS, PIVOT |
| Set | UNION, UNION ALL, INTERSECT, EXCEPT |

## SELECT

SELECT → Display data

```sql
SELECT * FROM EMP;
