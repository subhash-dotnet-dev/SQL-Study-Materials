# 📝 Day 17 — Quick Notes
## ISNULL() & Analytical / Window Functions

---

## 1. ISNULL()

### Definition

`ISNULL()` is used to handle `NULL` values by returning an alternative value when the first expression is `NULL`.

### Syntax

```sql
ISNULL(expression, replacement_value)
