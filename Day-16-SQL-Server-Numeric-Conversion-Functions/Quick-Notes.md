# 📝 Day 16 — SQL Server Numeric & Conversion Functions — Quick Notes

## 📌 Overview

Day 16 focuses on **Numeric/Mathematical Functions** and **Conversion Functions** in SQL Server.

### Functions Covered

- `ROUND()`
- `FLOOR()`
- `CEILING()`
- `CAST()`
- `CONVERT()`

---

# 🔢 1. ROUND()

### Definition

`ROUND()` is used to round a number to a specified number of decimal places.

### Syntax

```sql
ROUND(number, decimal_places)
