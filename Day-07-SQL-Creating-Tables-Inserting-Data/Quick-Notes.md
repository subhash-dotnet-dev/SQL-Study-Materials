# Day 07 — Quick Notes

## CREATE TABLE

CREATE TABLE → Creates table structure / metadata.

```sql
CREATE TABLE EMP
(
    EMPID TINYINT,
    ENAME VARCHAR(10),
    JOB VARCHAR(10),
    SAL SMALLMONEY,
    HIREDATE DATE,
    DEPT VARCHAR(10)
)
